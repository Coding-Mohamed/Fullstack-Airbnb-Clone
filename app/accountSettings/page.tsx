import React from "react";
import AccountSettings from "../_components/AccountSettings"; // Adjusted path

const AccountSettingsPage: React.FC = () => {
  return <AccountSettings />;
};

export default AccountSettingsPage;
